{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "bin/start.js",
  "scripts": {
    "start": "node ./bin/start.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "{{nodeVersion}}"
  },
  "dependencies": {
    {{dependencies|raw}}
  }
}
